import React from "react";
import logo from "../src/logo.jpg";

function Header() {
  return (
    <header style={styles.header}>
    </header>
  );
}

const styles = {
  header: {
    backgroundColor: "#ad7b7bff",
    padding: "20px 0",
    textAlign: "center",
    color: "white",
  },
};
export default Header;